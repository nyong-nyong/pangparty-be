package nyongnyong.pangparty.service.hashtag;

public class HashtagServiceImpl implements HashtagService {
    @Override
    public Long addHashtag(String hashtagName) {    // 이름의 해시태그 있는지 확인 후 없으면 넣는다.

        return null;
    }
}
